import React from "react";
import { withLayout } from "../../layout/Layout";

const CoursesPage = (): JSX.Element => {
  return <div>Welcome to the wonderful courses</div>;
};

export default withLayout(CoursesPage);
